defaultNamespace: authentik
helm:
  version: 2025.10.2
  chart: authentik
  repo: https://charts.goauthentik.io
  releaseName: authentik
labels:
  app: authentik
valuesFiles:
  - values.yaml
overrideTargets:
  - clusterSelector:
      matchLabels:
        needs-sso: true