apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-basicauth
  namespace: cattle-system
type: kubernetes.io/basic-auth
data:
  password: {{ required ".Values.token is required" .Values.token | b64enc }}
  username: {{ required ".Values.username is required" .Values.username | b64enc }}
