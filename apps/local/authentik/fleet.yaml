defaultNamespace: authentik
helm:
  version: 2025.10.2
  chart: authentik
  repo: https://charts.goauthentik.io
  releaseName: authentik
  valuesFiles:
    - values.yaml
labels:
  app: authentik
overrideTargets:
  - clusterSelector:
      matchLabels:
        needs-sso: true