
authentik:
    bootstrap_password: "!nfiniteP0wer"
    bootstrap_token: "auth-token-1234-5678-90ab-cdef"
    bootstrap_email: "admin@example.com"
    secret_key: "PleaseGenerateASecureKey"
    # This sends anonymous usage-data, stack traces on errors and
    # performance data to sentry.io, and is fully opt-in
    error_reporting:
        enabled: false
    postgresql:
        password: "ThisIsNotASecurePassword"

server:
    ingress:
        # Specify kubernetes ingress controller class name
        ingressClassName: traefik
        enabled: ${if hasKey .ClusterAnnotations "hosted-domain"}true${else}false${end}
        hosts:
            - ${if hasKey .ClusterAnnotations "hosted-domain"}sso.${if hasKey .ClusterAnnotations "cluster-name"}${get .ClusterAnnotations "cluster-name"}${else}${.ClusterName}${end}.${ get .ClusterAnnotations "hosted-domain" }${ end}

postgresql:
    enabled: true
    auth:
        password: "ThisIsNotASecurePassword"